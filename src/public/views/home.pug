html(lang="en")
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible", content="IE=edge")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    link(rel="stylesheet", href="../public/css/main.css")
    title Document
  body 
    header
      h1 WELCOME TO THE JUNGLE 🌿
    main
      div#welcome 
        h1 ROOM 🚪
        form
          input(type="text", placeholder="type room name")
          button Enter
      div#call
        div.stream
          div#myStream
            video#myFace(autoplay, playsinline, width="350", height="350")
            div#streamOption
              button#audio Audio On
              button#camera Camera On
              select#cameras
          video#peerFace(autoplay, playsinline, width="350", height="350")
        div#chatRoom
          h1 CHATTING ROOM 🌈
          div#chat
            ul
          form
            input(type="text", placeholder="message")
    script(src="/socket.io/socket.io.js") 
    script(src="../public/js/app.js") 